import { ExportToDirective } from './export-to.directive';

describe('ExportToDirective', () => {
  it('should create an instance', () => {
    const directive = new ExportToDirective();
    expect(directive).toBeTruthy();
  });
});
