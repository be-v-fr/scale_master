import { SearchCurrentDirective } from './search-current.directive';

describe('SearchCurrentDirective', () => {
  it('should create an instance', () => {
    const directive = new SearchCurrentDirective();
    expect(directive).toBeTruthy();
  });
});
