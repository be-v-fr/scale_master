/**
 * Note names provide three possible values to associate with
 * an (absolute) note interval, where the note accidental is
 * being used as the key.
 */
export interface NoteName {
    natural: string
    sharp: string
    flat: string
}